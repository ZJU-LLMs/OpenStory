用户目标：{{user_goal}}
世界类型：{{world_type_summary}}
世界标签：{{tags}}

世界地点集合：{{location_types}}
人物身份集合：{{character_roles}}
宏观规则：{{macro_rules}}

请制定世界内容的生成步骤计划，步骤按逻辑顺序排列。
计划步骤应与上方地点、人物身份、规则集合对应，确保每类核心要素都有对应的生成步骤。

每个步骤包含：
- name：步骤标识符（snake_case，使用 generate_ 前缀）
- target：该步骤要生成的内容描述（中文，具体说明生成什么，可列举关键实例）

通常顺序：世界背景 → 地点 → 角色 → 关系 → 规则 → 动作，可根据世界类型增减。
