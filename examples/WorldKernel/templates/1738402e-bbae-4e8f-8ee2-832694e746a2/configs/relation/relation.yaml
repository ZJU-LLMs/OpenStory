# ============================================================
#  Relation 全局配置模版
#  定义关系实体的维度组成
# ============================================================

name: Relation
entity_type: relation

dimensions:

  # ── 关系边 ────────────────────────────────────────────────
  edge:
    edge:
      type: EdgeDim
      path: dims/edge.yaml
